e.dt.on('draw', function () {
          let pageNumbers = [];
          $(e.el.nativeElement)
            .find('.paginate_button')
            .not('.previous, .next')
            .each(function () {
              pageNumbers.push($(this).text());
            });

          console.log("Visible Page Numbers:", pageNumbers);
